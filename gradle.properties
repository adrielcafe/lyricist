org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.unsafe.configuration-cache=false

org.jetbrains.compose.experimental.macos.enabled=true
org.jetbrains.compose.experimental.uikit.enabled=true
org.jetbrains.compose.experimental.jscanvas.enabled=true
org.jetbrains.compose.experimental.wasm.enabled=true

android.useAndroidX=true
android.enableJetifier=false
android.defaults.buildfeatures.buildconfig = false

# KSP 2.0 and Kotlin 2.2.21 configurations
kotlin.code.style=official
kotlin.native.disableCompilerDaemon=true
kotlin.native.ignoreDisabledTargets=true
kotlin.native.ignoreIncorrectDependencies=true
kotlin.mpp.stability.nowarn=true
kotlin.mpp.androidSourceSetLayoutVersion=2

# Kotlin Multiplatform memory model
kotlin.native.binary.memoryModel=experimental

# KSP 2.0 explicit configuration for optimal performance and multiplatform compatibility
# Enable KSP 2.0 architecture for K2 compiler compatibility and performance improvements
ksp.useKsp2=true

# Enable incremental compilation for dramatically faster builds (20-50% improvement)
# Only reprocesses changed files instead of regenerating everything from scratch
ksp.incremental=true

# Allow KSP processors to work across all multiplatform targets
# Prevents "processor not found" errors on iOS, Desktop, Web, and native targets
ksp.allow.all.target.platforms=true

# Disable incremental compilation logging for cleaner build output
# Enable temporarily (ksp.incremental.log=true) when debugging KSP issues
ksp.incremental.log=false

# KSP Development/Debugging options (uncomment when needed)
# Enable detailed incremental compilation logging and file change tracking
#ksp.incremental.log=true
# Allow processing files with syntax errors (useful for IDE integration)
#ksp.allow.syntax.errors=false
# Show detailed processor output and generation steps
#ksp.verbose=true

# Optional: Experimental hierarchical processing model for enhanced incremental compilation
# Can provide additional performance improvements in complex multiplatform projects
#ksp.experimental.processing.model=hierarchical

# KSP warnings suppression for known compatibility issues
kotlin.suppressGradlePluginWarnings=IncorrectCompileOnlyDependencyWarning

# Maven
SONATYPE_HOST=S01
SONATYPE_AUTOMATIC_RELEASE=true
RELEASE_SIGNING_ENABLED=false

GROUP=cafe.adriel.lyricist

POM_DESCRIPTION=The missing I18N/L10N library for Jetpack Compose!
POM_INCEPTION_YEAR=2021
POM_URL=https://github.com/adrielcafe/lyricist

POM_LICENCE_NAME=The MIT License
POM_LICENCE_URL=https://opensource.org/licenses/MIT
POM_LICENCE_DIST=repo

POM_SCM_URL=https://github.com/adrielcafe/lyricist
POM_SCM_CONNECTION=scm:git:ssh://git@github.com/adrielcafe/lyricist.git
POM_SCM_DEV_CONNECTION=scm:git:ssh://git@github.com/adrielcafe/lyricist.git

POM_DEVELOPER_ID=adrielcafe
POM_DEVELOPER_NAME=Adriel Cafe
POM_DEVELOPER_URL=https://github.com/adrielcafe/